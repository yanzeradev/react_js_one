import React from 'react';

function RecipeTitle() {
        const title = "Receita de bolo"
    return (
        <div> 
            <h1>{title}</h1>
        </div>
    );
}

export default RecipeTitle;